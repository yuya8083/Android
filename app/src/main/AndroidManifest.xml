<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yuya0817.ReviveSeat">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.MOCK_ACCESS_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Revive Seat"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Light.NoTitleBar">
        <activity android:name=".Load">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main2Activity" />
        <activity android:name=".Main3Activity" />
        <activity android:name=".starbucks" />
        <activity android:name=".Select_find_method" />
        <activity android:name=".Location_cafename" />
        <activity android:name=".Category_list" />
        <activity android:name=".Main4Activity" />
        <activity android:name=".qrcode" />
        <activity android:name=".MakeAccount" />
        <activity android:name=".TableSet" />
        <activity android:name=".Evaluation" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".wait" />
        <activity android:name=".arrive_wait" />
        <activity android:name=".Confirmation" />
        <activity android:name=".Sharing" />
        <activity android:name=".select_share_sheet_host" />
        <activity android:name=".select_share_sheet_guest" />
        <activity
            android:name=".matching"
            android:theme="@style/Theme.ChildTranslucent" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".JoinConfirmation"
            android:label="@string/title_activity_map_confirmation" />
        <activity android:name=".share_table_list" />
        <activity android:name=".chat_system" />
        <activity android:name=".map_window" />
        <activity android:name=".orner_wait" />
        <activity android:name=".JoinQrcode" />
        <activity android:name=".JoinSharing" />
        <activity android:name=".JoinEvaluation" />
        <activity android:name=".Check_in" />
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />

        <service android:name=".MyIntentService" />

        <activity android:name=".MainActivity" />
        <activity android:name=".NewUserSet" />
        <activity android:name=".Top" />
        <activity
            android:name=".TableSearch"
            android:label="@string/title_activity_table_search" />
        <activity android:name=".Login2" />
        <activity android:name=".select_menu_food" />
        <!--
 ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".LoginPage" />
    </application>

</manifest>